interface:
  display_name: "Project Init"
  short_description: "Create lean repository agent instructions"
  default_prompt: "Use $project-init to create or audit this repository's AGENTS.md."

policy:
  allow_implicit_invocation: false
